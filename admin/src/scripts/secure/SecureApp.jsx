import React, { Component } from 'react';
class SecureApp extends Component {
  render() {
    return (
      <React.Fragment>
        Secured
      </React.Fragment>
    );
  }
}

export default SecureApp;
